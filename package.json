{
  "name": "compare-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
